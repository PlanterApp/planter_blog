[[headers]]
  # Define which paths this specific [[headers]] block will cover.
  for = "/posts/*"
    [headers.values]
    Access-Control-Allow-Origin = "https://gardenplanter.app"
    Access-Control-Allow-Methods = "GET"
